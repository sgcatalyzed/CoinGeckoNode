{
  "name": "node-starter",
  "version": "0.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "coingecko-api": "^1.0.10"
  },
  "devDependencies": {
    "nodemon": "^2.0.15"
  }
}
